package seedu.address.model.util;

import seedu.address.model.ReadOnlyShortcut;
import seedu.address.model.Shortcut;

/**
 * Contains utility methods for populating {@code Shortcut} with sample data.
 */
public class SampleShortcutUtil {

    public static ReadOnlyShortcut getSampleShortcut() {
        return new Shortcut();
    }

}
